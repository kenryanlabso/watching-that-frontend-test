import styled from 'styled-components';

const LoaderStyles = {
  Container: styled.div`
    margin-top: 15%;

    .sk-folding-cube {
      height: 45px;
      width: 45px;
    }
  `,
};

export default LoaderStyles;
